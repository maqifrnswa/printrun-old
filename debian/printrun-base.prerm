#!/bin/sh

set -e

case "$1" in
	remove|upgrade|remove-in-favour|deconfigure-in-favour)
		#
		# do here pre removes
		#
		echo "the compiled files"
		rm /usr/share/printrun/*.pyc
		rm /usr/share/printrun/*.pyo
		;;
	failed-upgrade)
		;;
esac

exit 0
